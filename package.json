{
  "name": "zenetic-web-development",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:ssg": "npm run build:client && npm run build:server && npm run generate",
    "build:client": "vite build --outDir dist/static --ssrManifest",
    "build:server": "vite build --ssr entry-server.tsx --outDir dist/server",
    "generate": "node prerender.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.38.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-router": "^6.22.0",
    "react-router-dom": "^6.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}